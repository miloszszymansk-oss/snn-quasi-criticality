"""CriticalCortex — spec-first spiking-network criticality engine.

Milestone M0 exposes only the numeric reference (`criticalcortex.numerics`);
the connectome builder, Rust routing kernel, and analysis pipeline arrive in M1+.
"""

__version__ = "0.2.0"
